version: "3"

vars:
  WAVS_DOCKER_IMAGE: 'ghcr.io/lay3rlabs/wavs:local' # TODO: fix this back to original. For now, https://github.com/Lay3rLabs/WAVS/pull/905
  MIDDLEWARE_DOCKER_IMAGE: 'ghcr.io/lay3rlabs/wavs-middleware:latest{{if eq .ARCH "arm64"}}-arm64{{end}}'
  FOUNDRY_DOCKER_IMAGE: "ghcr.io/foundry-rs/foundry:latest"
  JAEGER_DOCKER_IMAGE: "jaegertracing/jaeger:2.5.0"
  PROMETHEUS_DOCKER_IMAGE: "prom/prometheus:v3.3.0"
