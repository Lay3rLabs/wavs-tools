# This is just a convenience to start common tasks from the root directory
version: "3"

# silent: true

dotenv: ['.env']

includes:
  backend: ./backend/Taskfile.yml
  cli: ./cli/Taskfile.yml

tasks:
  default:
    cmds:
      - task --list-all