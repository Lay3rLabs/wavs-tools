{
  "node-option": ["import=tsx"],
  "extensions": ["ts"],
  "spec": "src/**/*.test.ts",
  "recursive": true,
  "timeout": 5000,
  "reporter": "mochawesome",
  "reporter-options": "reportDir=.test-reports,reportFilename=test-report,json=true,overwrite=true"
}